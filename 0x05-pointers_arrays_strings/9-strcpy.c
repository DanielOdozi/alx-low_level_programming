#include "main.h"
#include <string.h>

/**
 *_strcpy - copy strg to another string
 * @dest: pointer
 * @src: poiter
 * Return: numbers dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);

}
